ZBUILD=zbuild

REGISTRY_PATH=/tmp/net.dn42.registry/data/inetnum
OUT_PATH=${PWD}`dirname ${0} | sed -r 's/^[.]{1,2}//'`/build

DEFAULT_TTL=7200
CONTACT_EMAIL="nixnodes.nic.dn42"

SERVER_NAME_TIER0="nixnodes.root-servers.dn42"
SERVER_NAME_TIER1="nixnodes.zone-servers.dn42"

MASTERS="172.22.177.2;"